# LLM_Preprocessor
LLM_Preprocessor automates large-scale document cleanup for LLM pipelines. It classifies files, extracts text, runs Tesseract OCR, handles scanned documents, and organizes outputs into structured folders. CPU-optimized, memory-safe, and dependency-light for consistent preprocessing.
